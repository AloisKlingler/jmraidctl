Building
--------
  make

Requirements: kernel headers with SG_IO ioctl (kernel-devel on RHEL/CentOS).

Running
-------
Execute as root or a user with write access to the SCSI generic device:

  ./jmraidctl /dev/sgX

Find your RAID controller with: ls -l /dev/disk/by-id/ | grep JMicron
